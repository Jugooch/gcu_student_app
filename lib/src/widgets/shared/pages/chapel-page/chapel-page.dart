import 'package:flutter/material.dart';

class ChapelPage extends StatelessWidget {
  const ChapelPage({Key? key}) : super(key: key);

@override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: null,
      body: Center(
        child: ListView(
          children: const [
          ],
        ),
      ),
    );
  }
}